import React from "react";

const Outcomes = ({ demoRef }) => {
  return (
    <div className="relative md:py-12 py-8 px-6 max-w-full bg-subjectsBg min-h-[40vh]"></div>
  );
};

export default Outcomes;
